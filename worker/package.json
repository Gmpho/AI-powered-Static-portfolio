{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@google/generative-ai": "latest",
		"copy-of-ai-powered-portfolio": "file:.."
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "latest",
		"typescript": "latest",
		"vitest": "latest",
		"wrangler": "latest"
	}
}
